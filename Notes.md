# Comandos útiles git

---

Crear tag

```cmd
git tag -a <nombre_etiqueta> -m <description>
```

Subir un tag en específico
```cmd
git push origin <nombre_etiqueta>
```

Subir tags

```cmd
git push --tags
```
